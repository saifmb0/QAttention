# OFFICIAL COMMENT FOR REVIEWER LYDb

We thank the reviewer for the careful reading and for raising several important technical questions. We address each point below.

### 1. Table 2 and the reported end-to-end speedup

The reviewer correctly identified an error in the Table 2 column label. The values shown as "Verify ms" (0.547–0.683) correspond to the **fraction of total step time spent in verifier attention**, not milliseconds. The underlying benchmark logs contain the measured verifier latencies used to compute the reported speedups.

For completeness, the measured verifier latencies are shown below.

| Context Length | Method | Throughput (tok/s) | Verify Latency (ms) | Verify Fraction (%) | E2E Speedup |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **2048** | FlashInfer (monolithic) | 29.63 ± 0.47 [29.30,30.30] | 72.85 ± 0.44 [72.06,73.41] | 54.69% ± 0.18% [54.47,54.90] | — |
| | QAttention (hybrid) | 33.33 ± 0.82 [32.40,34.40] | 57.75 ± 0.29 [57.26,58.11] | 48.75% ± 0.14% [48.58,48.92] | **1.12 × ±0.01 [1.11,1.14]** |
| **4096** | FlashInfer (monolithic) | 9.53 ± 0.19 [9.40,9.80] | 96.01 ± 0.90 [94.65,97.40] | 61.29% ± 0.09% [61.18,61.39] | — |
| | QAttention (hybrid) | 12.47 ± 0.39 [12.10,13.00] | 59.22 ± 0.29 [58.69,59.54] | 49.23% ± 0.28% [48.86,49.54] | **1.31 × ±0.02 [1.29,1.33]** |
| **6144** | FlashInfer (monolithic) | 6.87 ± 0.17 [6.70,7.10] | 121.83 ± 0.87 [120.88,123.27] | 64.76% ± 0.24% [64.55,65.10] | — |
| | QAttention (hybrid) | 9.93 ± 0.26 [9.70,10.30] | 64.54 ± 0.15 [64.24,64.68] | 49.50% ± 0.38% [49.01,49.94] | **1.45 × ±0.00 [1.44,1.45]** |
| **8192** | FlashInfer (monolithic) | 5.60 ± 0.14 [5.50,5.80] | 153.06 ± 0.47 [152.28,153.74] | 67.17% ± 0.07% [67.09,67.27] | — |
| | QAttention (hybrid) | 8.80 ± 0.22 [8.60,9.10] | 71.73 ± 0.12 [71.51,71.85] | 49.21% ± 0.42% [48.67,49.68] | **1.57 × ±0.01 [1.56,1.58]** |
| **10240** | FlashInfer (monolithic) | 4.63 ± 0.12 [4.50,4.80] | 176.17 ± 0.55 [175.31,176.98] | 68.32% ± 0.14% [68.13,68.45] | — |
| | QAttention (hybrid) | 7.47 ± 0.24 [7.30,7.80] | 78.74 ± 0.13 [78.57,78.89] | 49.20% ± 0.29% [48.79,49.46] | **1.61 × ±0.02 [1.59,1.62]** |

At **L = 10240**, verifier latency decreases from **176.2 ms** to **78.7 ms**, while total decoding time decreases from **257.9 ms** to **160.0 ms**, yielding an Amdahl-predicted speedup of **257.9 / 160.0 = 1.61×**, matching the measured throughput improvement from **4.64 tok/s** to **7.45 tok/s**.

The labeling error does not affect the reported measurements or conclusions; it affects only the unit displayed in that column.

---

### 2. Shared prefix KV cache

QAttention does not attend only to tree nodes.

Verification is implemented as a **two-pass split-softmax pipeline**.

* The shared prefix is processed using standard FlashAttention over the prefix KV cache.
* The speculative tree is processed using the ancestor-sparse QAttention kernel.
* The two partial attention results are merged using FlashAttention-2's online log-sum-exp formulation.

This preserves exact attention semantics while allowing only the tree portion of the computation to exploit ancestor sparsity.

Correctness is verified against PyTorch SDPA over the complete prefix-plus-tree attention mask, demonstrating numerical equivalence for both prefix and tree attention.

---

### 3. Dynamic trees

The reported parameters correspond to EAGLE-3's dynamic tree construction.

The quantities **b** and **d** denote the maximum branching factor and maximum search depth, while **tt** denotes the realized number of speculative tokens after probability pruning.

Consequently, **b = 20**, **d = 20**, and **tt = 343** are not inconsistent. They describe the search limits and the realized tree after pruning rather than a complete regular tree.

QAttention operates directly on an explicit parent array and therefore supports arbitrary ragged trees without assuming a regular topology.

---

### 4. Hardware efficiency of ancestor gathers

Although QAttention replaces dense KV sweeps with ancestor gathers, the working set remains very small.

For the evaluated configurations, the unique ancestor KV working set is less than **1 MB**, allowing it to reside almost entirely in GPU L2 cache. Profiling shows L2 hit rates above **92%**, substantially reducing DRAM traffic despite the irregular access pattern.

Consequently, the reduction in logical memory accesses translates into measured bandwidth and latency improvements rather than being offset by uncoalesced memory accesses.

---

# OFFICIAL COMMENT FOR REVIEWER tRm3

We thank the reviewer for the detailed evaluation and for clearly identifying the evidence that would most strengthen the paper.

### 1. Deployment-relevant operating points

The paper emphasizes long-context workloads because verifier attention increasingly dominates decoding time as context grows. However, QAttention is not limited to extreme operating points.

Representative verifier benchmarks at moderate context lengths and moderate tree sizes are shown below.

| Context | Tree Size | Configuration | FlashInfer | QAttention | Speedup |
| ------: | --------: | :------------ | ---------: | ---------: | ------: |
|    1024 |       110 | b=8,d=16      |  0.0850 ms |  0.0543 ms |   1.57× |
|    1024 |       165 | b=8,d=24      |  0.1812 ms |  0.0879 ms |   2.06× |
|    1024 |       165 | b=24,d=8      |  0.1812 ms |  0.0844 ms |   2.15× |
|    4096 |       110 | b=8,d=16      |  0.2714 ms |  0.2243 ms |   1.21× |
|    4096 |       165 | b=8,d=24      |  0.6676 ms |  0.3251 ms |   2.05× |
|    4096 |       165 | b=24,d=8      |  0.6675 ms |  0.3225 ms |   2.07× |

These results demonstrate that the kernel advantage is present well before the largest trees evaluated in the paper. As context length increases, verifier attention becomes an increasingly dominant component of total decoding time, making reductions in verifier cost progressively more valuable.

---

### 2. Significance

We agree that today's speculative decoding systems typically operate with relatively shallow trees. Our interpretation is that this reflects **two independent bottlenecks** rather than a single limitation.

The first is verifier complexity. Existing verifier kernels scale quadratically with tree size, making larger trees increasingly expensive regardless of acceptance rate. QAttention removes this systems bottleneck by reducing verifier complexity from **O(N²)** to **O(N·d)** while preserving exact attention semantics.

The second is acceptance-rate degradation. As discussed in the paper, current draft models exhibit declining acceptance as tree depth increases, this is fundamentally a modeling problem, and there has been no need for higher acceptance rates because of the quadratic complexity of tree verification. Draft objectives, reward functions, and speculative decoding strategies continue to evolve, and improvements in draft quality can directly improve acceptance.

We therefore view these two bottlenecks as complementary. QAttention does not claim to solve acceptance decay. Rather, it removes the computational barrier that would otherwise prevent future improvements in draft quality from translating into end-to-end inference gains.

Additionally, Reviewer tRm3 notes that acceptance decay often limits optimal tree sizes in standard draft setups. However, acceptance rate behavior varies heavily with the tree construction algorithm. Unlike standard sampling, Sequoia's DP tree optimizer explicitly scales tree size to $N=1024$ because its tree search continues to increase the expected accepted tokens per step. In Sequoia, large trees directly correspond to higher accepted tokens per step. QAttention eliminates the $O(N^2)$ verifier cost, achieving up to 1.41x at N=1024.

More broadly, the algorithm is not specific to speculative decoding. It exploits ancestor-sparse attention, a property shared by a broader class of tree-structured computations. We focus on speculative decoding because it is currently the most consequential deployment setting, but the same execution strategy is applicable wherever attention follows ancestor-defined dependency graphs.

---

### 3. Originality

We agree that the observation that a query attends only to its ancestors is conceptually straightforward.

Our contribution is demonstrating that this sparsity can be realized efficiently inside an exact FlashAttention-compatible online softmax implementation for **dynamic ragged trees**, while preserving mathematical equivalence with standard attention.

Accordingly, we view QAttention primarily as a systems contribution rather than a new attention formulation.

---

### 4. DeFT comparison

Our comparison evaluates the published **DeFT** verifier kernel (Yao et al., ICLR 2025).

The benchmark imports and measures **DeFT_decode** from the implementation distributed with the FastTree benchmarking artifact. The measured baseline is therefore the DeFT verification kernel itself rather than FastTree's radix-tree attention implementation.

DeFT performs additional grouping and reduction operations before attention. QAttention instead operates directly on the ragged parent representation using a single ancestor walk, eliminating those preprocessing stages while preserving exact outputs.

---

### 5. Statistical stability

Across repeated benchmark runs, latency variation remained within approximately **±1.2%**, and the relative ordering of all methods remained unchanged, indicating that the reported improvements are stable.

---

### 6. Generality

To evaluate generality, we additionally benchmarked QAttention across multiple deployment settings.

* **Model Architectures**: Evaluated across Llama-3.2-3B-Instruct ($H = 24$, 28 layers), Llama-3.1-8B-Instruct ($H = 32$, 32 layers), and Qwen-2.5-7B-Instruct ($H = 28$, 28 layers). Measured single-layer and full model verifier attention latencies at $N = 1024$ are:

| Model Name | Config ($b, d$) | Context Len $L$ | QA Layer (ms) | QA Model (ms) |
| :--- | :--- | :--- | :--- | :--- |
| **Llama-3.2-3B-Instruct** | $b=10, d=7$ | 4096 | 0.102 ms | 2.85 ms |
| | $b=20, d=20$ | 4096 | 0.101 ms | 2.83 ms |
| | $b=20, d=20$ | 8192 | 0.102 ms | 2.87 ms |
| | $b=20, d=20$ | 10240 | 0.100 ms | **2.81 ms** |
| **Qwen-2.5-7B-Instruct** | $b=10, d=7$ | 4096 | 0.107 ms | 3.01 ms |
| | $b=20, d=20$ | 4096 | 0.107 ms | 3.01 ms |
| | $b=20, d=20$ | 8192 | 0.105 ms | 2.95 ms |
| | $b=20, d=20$ | 10240 | 0.105 ms | **2.95 ms** |
| **Llama-3.1-8B-Instruct** | $b=10, d=7$ | 4096 | 0.113 ms | 3.62 ms |
| | $b=20, d=20$ | 4096 | 0.113 ms | 3.60 ms |
| | $b=20, d=20$ | 8192 | 0.112 ms | 3.57 ms |
| | $b=20, d=20$ | 10240 | 0.115 ms | **3.66 ms** |

* **RTX 4000 Ada with W4A16 GPTQ**: Measured end-to-end throughput is:

| Context |     Baseline |   QAttention | Speedup |
| ------: | -----------: | -----------: | ------: |
|    1024 | 16.83 ± 0.24 | 21.50 ± 0.28 |   1.28× |
|    2048 |  9.20 ± 0.14 | 12.97 ± 0.19 |   1.41× |
|    4096 |  3.00 ± 0.00 |  5.20 ± 0.00 |   1.73× |

These results indicate that the observed improvements are not tied to a single hardware platform or precision configuration.

---

# OFFICIAL COMMENT FOR REVIEWER 7FHT

We thank the reviewer for the constructive assessment and suggestions.

Regarding generality, QAttention was evaluated beyond a single benchmark configuration. We integrated the kernel into both **EAGLE-3** and **Sequoia**, evaluated multiple Llama-family configurations, and benchmarked on both **A100** and **RTX 4000 Ada** GPUs. Additional experiments under W4A16 GPTQ quantization also exhibit consistent speedups, indicating that the performance improvements are not specific to one hardware platform or precision format.

Regarding practical workloads, our central claim is not that today's draft models already favor very large speculative trees. Rather, we identify verifier complexity as an independent systems bottleneck that limits exploration of larger tree structures. By reducing verification from quadratic to ancestor-linear complexity while preserving exact attention semantics, QAttention removes one of the principal computational barriers to scaling tree-based decoding. Future improvements in draft-model quality can therefore translate directly into inference gains without remaining constrained by verifier cost.

Finally, because QAttention changes only the execution schedule of attention while preserving the underlying computation, it is mathematically identical to the baseline verifier. Consequently, generation outputs and draft acceptance behavior remain unchanged.

---

# OFFICIAL COMMENT FOR THE AREA CHAIR

We thank the Area Chair and all reviewers for the detailed and constructive discussion.

The reviewer feedback primarily focused on correctness, evaluation, and significance.

Regarding correctness, we clarified the labeling issue in Table 2 and provided the underlying verifier latency measurements, which satisfy the reported Amdahl speedups when computed from the measured step times. We also clarified how the shared prefix is incorporated through a two-pass split-softmax pipeline and explained how QAttention naturally supports dynamic ragged trees using explicit parent representations.

Regarding evaluation, we provided deployment-oriented benchmark results at moderate context lengths and tree sizes, clarified that the DeFT comparison evaluates the published DeFT verification kernel, and reported repeated-run statistics demonstrating stable performance across runs. Additional measurements on RTX 4000 Ada under W4A16 quantization indicate that the observed improvements are not limited to a single hardware or precision configuration.

Regarding significance, our contribution is not that QAttention alone resolves acceptance-rate degradation. Rather, we argue that practical tree scaling is currently constrained by two independent factors: verifier complexity and draft-model quality. QAttention removes the former by reducing verifier attention from quadratic to ancestor-linear complexity while preserving exact attention semantics. This removes an important systems barrier and allows future advances in draft modeling to translate directly into end-to-end inference improvements rather than remaining masked by verification overhead.

Finally, although speculative decoding is the motivating application, the underlying execution strategy applies to a broader class of ancestor-sparse tree attention problems. We therefore view QAttention as a general systems contribution for efficiently executing dynamic tree-structured attention while preserving exact computation.

